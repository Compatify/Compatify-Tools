{
  "name": "compatify-api-proxy",
  "version": "1.0.0",
  "description": "A serverless function to proxy requests for the Gemini API.",
  "main": "api/proxy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/generative-ai": "^0.11.3"
  }
}
